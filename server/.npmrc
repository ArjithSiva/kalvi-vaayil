# Render builds this directory as its own root. A committed lockfile that drifts
# out of sync with package.json is what produced the npm Arborist crash
# ("Cannot read properties of null (reading 'matches')"), so never write one here.
package-lock=false
fund=false
audit=false
